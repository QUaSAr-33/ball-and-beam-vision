
---

### ✅ `python/README.txt`
```markdown
# Python Vision Tracking

This folder contains the vision-based ball position tracking script.

- **vision_tracking.py** – Main script that captures webcam frames, finds the ball center, applies a PID controller, and sends servo commands via serial.

### Run
```bash
pip install -r requirements.txt
python vision_tracking.py
